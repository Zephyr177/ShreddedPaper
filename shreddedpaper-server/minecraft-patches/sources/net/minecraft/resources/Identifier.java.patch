--- a/net/minecraft/resources/Identifier.java
+++ b/net/minecraft/resources/Identifier.java
@@ -26,6 +_,10 @@ public final class Identifier implements Comparable<Identifier> {

     public static final String PAPER_NAMESPACE = "paper"; // Paper

     private final String namespace;

     private final String path;

+    // ShreddedPaper start - Cache identifier toString and hash

+    private String identifierStr;

+    private int hash;

+    // ShreddedPaper end - Cache identifier toString and hash

 

     private Identifier(final String namespace, final String path) {

         assert isValidNamespace(namespace);

@@ -131,7 +_,14 @@ public final class Identifier implements Comparable<Identifier> {

 

     @Override

     public String toString() {

-        return this.namespace + ":" + this.path;

+        // ShreddedPaper start - Cache identifier toString and hash

+        String identifierStr = this.identifierStr;

+        if (identifierStr == null) {

+            identifierStr = this.namespace + ":" + this.path;

+            this.identifierStr = identifierStr;

+        }

+        return identifierStr;

+        // ShreddedPaper end - Cache identifier toString and hash

     }

 

     @Override

@@ -141,7 +_,14 @@ public final class Identifier implements Comparable<Identifier> {

 

     @Override

     public int hashCode() {

-        return 31 * this.namespace.hashCode() + this.path.hashCode();

+        // ShreddedPaper start - Cache identifier toString and hash

+        int hash = this.hash;

+        if (hash == 0) {

+            hash = 31 * this.namespace.hashCode() + this.path.hashCode();

+            this.hash = hash;

+        }

+        return hash;

+        // ShreddedPaper end - Cache identifier toString and hash

     }

 

     @Override

